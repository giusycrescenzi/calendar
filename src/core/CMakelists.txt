set(CORE_SRC
    Day.cpp
    Register.cpp
    Task.cpp
)

set(CORE_H
    Day.h
    Register.h
    Task.h
)

add_library(corelib ${CORE_SRC} ${CORE_H})

# include directories for this library
target_include_directories(corelib 
    PUBLIC 
        ${CMAKE_CURRENT_SOURCE_DIR}  # so others can include core headers
)